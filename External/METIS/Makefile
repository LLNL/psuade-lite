

default:
	(cd Lib ; make ) 
#(cd Programs ; make ) 
#(cd Test ; make ) 

clean:
	(cd Lib ; make clean )
#(cd Programs ; make clean )
#(cd Test ; make clean )
	(rm -f *.a )

realclean:
	(cd Lib ; make realclean )
#(cd Programs ; make realclean )
#(cd Test ; make realclean )
	(rm -f *.a )

